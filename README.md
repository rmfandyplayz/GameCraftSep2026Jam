balls

AI usage notice--AI coding tools were utilized to aid development.\
unless otherwise mentioned, AI was only used for the following:
- parts of the UI code. AI was prompted to build a data-driven, modular animation system using DOTween, so UI animations could be made quickly instead of having to write a framework from scratch every single time, which is likely to be buggy and error-prone
